import Heading from '../Heading/Heading';
import './Header.css';

const Header = () => {
  return (
    <header className='header'>
      <Heading title='Lists and Keys in React' />
    </header>
  );
};

export default Header;
